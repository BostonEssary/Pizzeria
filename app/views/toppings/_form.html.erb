<%= form_with model: topping do |form| %>

    <div>
        <%= form.label :name%>
        <%= form.text_field :name%>
    </div>
    <h3><%= form.label "Toppings"%></h3>

    <div>
        <%= form.submit%>
    </div>
    <% if @topping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topping.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @topping.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>